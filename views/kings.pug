extends layout

block content
  h1= title
  p Welcome to #{title}
  p hello world
  - var results = [name:string, kingdom:string, years_ruled:int   ]
  div(class = "container-fluid")
    div(class= "row")
      -results["Dree","Everything",2000]
       each item in results
         div(class="col-sm-2") #{results.name}
         div(class="col-sm-2") #{results.kingdom}
         div(class="col-sm-2") #{results.years_ruled}
       
